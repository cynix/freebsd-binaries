name: Containers

on:
  workflow_call:
    inputs:
      project:
        type: string
        required: true
      version:
        type: string
        required: false
      containers:
        type: string
        required: true

jobs:
  container:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        container: ${{ fromJSON(inputs.containers) }}
    permissions:
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Build
        uses: vmactions/freebsd-vm@v1
        env:
          INPUT_PROJECT: ${{ inputs.project }}
          INPUT_CONTAINER: ${{ matrix.container }}
        with:
          usesh: true
          sync: sshfs
          envs: INPUT_PROJECT INPUT_CONTAINER
          prepare: |
            set -ex

            sysctl net.inet.ip.forwarding=1

            mkdir -p /usr/local/etc/pkg/repos
            echo 'FreeBSD: { url: "pkg+https://pkg.freebsd.org/${ABI}/latest" }' > /usr/local/etc/pkg/repos/FreeBSD.conf
            pkg install -y podman-suite python3 uv

            truncate -s 32G /var/tmp/z
            mkdir -p /var/db/containers/storage
            zpool create -R /var/db/containers/storage -O mountpoint=/ -O compression=lz4 z /var/tmp/z

            buildah login -u ${{ github.actor }} -p ${{ github.token }} ghcr.io
          run: |
            uv run --frozen build-container

      - name: Cleanup
        uses: dataaxiom/ghcr-cleanup-action@v1
        with:
          package: ${{ matrix.container }}
          keep-n-tagged: 3
          exclude-tags: latest
          delete-untagged: true
          delete-ghost-images: true
          delete-partial-images: true
