diff --git i/build.rs w/build.rs
index 66ed2620..1401d440 100644
--- i/build.rs
+++ w/build.rs
@@ -5,6 +5,8 @@ use std::process::Command;
 
 fn zig_target(target: &str) -> &str {
     match target {
+        "x86_64-unknown-freebsd" => "x86_64-freebsd",
+        "aarch64-unknown-freebsd" => "aarch64-freebsd",
         "x86_64-unknown-linux-gnu" => "x86_64-linux-gnu",
         "aarch64-unknown-linux-gnu" => "aarch64-linux-gnu",
         "x86_64-unknown-linux-musl" => "x86_64-linux-musl",
diff --git i/src/platform/fallback.rs w/src/platform/fallback.rs
index 30bea5eb..fa88b45e 100644
--- i/src/platform/fallback.rs
+++ w/src/platform/fallback.rs
@@ -1,3 +1,4 @@
+use std::os::fd::RawFd;
 use std::path::PathBuf;
 use std::process::Command;
 
@@ -66,6 +67,11 @@ pub fn foreground_process_group_id(_child_pid: u32) -> Option<u32> {
     None
 }
 
+/// Unsupported platform stub.
+pub fn foreground_process_group_id_for_tty_fd(_fd: RawFd) -> Option<u32> {
+    None
+}
+
 /// Unsupported platform stub.
 pub fn process_cwd(_pid: u32) -> Option<PathBuf> {
     None
