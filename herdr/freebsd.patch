diff --git i/build.rs w/build.rs
index b7a6a005..0df23044 100644
--- i/build.rs
+++ w/build.rs
@@ -5,6 +5,8 @@ use std::process::Command;
 
 fn zig_target(target: &str) -> &str {
     match target {
+        "x86_64-unknown-freebsd" => "x86_64-freebsd",
+        "aarch64-unknown-freebsd" => "aarch64-freebsd",
         "x86_64-unknown-linux-gnu" => "x86_64-linux-gnu",
         "aarch64-unknown-linux-gnu" => "aarch64-linux-gnu",
         "x86_64-unknown-linux-musl" => "x86_64-linux-musl",
diff --git i/src/app/api/plugins/manifest.rs w/src/app/api/plugins/manifest.rs
index 657f8257..68ed3739 100644
--- i/src/app/api/plugins/manifest.rs
+++ w/src/app/api/plugins/manifest.rs
@@ -510,6 +510,8 @@ fn normalize_platforms(
 fn current_platform() -> PluginPlatform {
     if cfg!(target_os = "linux") {
         PluginPlatform::Linux
+    } else if cfg!(target_os = "freebsd") {
+        PluginPlatform::Linux
     } else if cfg!(target_os = "macos") {
         PluginPlatform::Macos
     } else {
diff --git i/src/platform/fallback.rs w/src/platform/fallback.rs
index cdfbc157..6d19f72d 100644
--- i/src/platform/fallback.rs
+++ w/src/platform/fallback.rs
@@ -1,3 +1,4 @@
+use std::os::fd::RawFd;
 use std::path::PathBuf;
 use std::process::Command;
 
@@ -173,6 +174,11 @@ pub fn foreground_process_group_id(_child_pid: u32) -> Option<u32> {
     None
 }
 
+/// Unsupported platform stub.
+pub fn foreground_process_group_id_for_tty_fd(_fd: RawFd) -> Option<u32> {
+    None
+}
+
 /// Unsupported platform stub.
 pub fn process_cwd(_pid: u32) -> Option<PathBuf> {
     None
